appName: smarter
workerName: smarter-worker
beatName: smarter-beat
appJobName: smarter-job-db-init
version: 0.7.2
deployment:
  containerPort: 8000
service:
  port: 8000
  targetPort: 8000
redis:
  fullnameOverride: "smarter-redis"
  global:
    redis:
      password: "smarter"
  replica:
    replicaCount: 1
  master:
    resources:
      requests:
        memory: "128Mi"
        cpu: "12m"
      limits:
        memory: "500m"
        cpu: "100m"
    affinity: {}
    tolerations:
      - key: "lawrencemcdaniel.com/smarter"
        operator: Equal
        value: "smarter-platform"
        effect: NoSchedule
    nodeSelector:
      environment: "smarter-platform"
