apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.workerName }}"
  namespace: "{{ .Values.env.NAMESPACE }}"

spec:
  selector:
    app.kubernetes.io/name: "{{ .Values.workerName }}"
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: {{ .Values.service.targetPort }}
  sessionAffinity: None
  type: ClusterIP
