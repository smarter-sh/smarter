apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    meta.helm.sh/release-name: {{ .Values.names.appName | default "smarter" }}-mariadb
    meta.helm.sh/release-namespace: {{ .Values.namespace }}
    meta.helm.sh/application-version: {{ .Values.version }}
  labels:
    App: {{ .Values.names.appName | default "smarter" }}-mariadb
  name: {{ .Values.names.appName | default "smarter" }}-mariadb
  namespace: {{ .Values.namespace }}

spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.names.mariadbName | default "smarter-mariadb" }}
      app.kubernetes.io/component: mariadb
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.names.mariadbName | default "smarter-mariadb" }}
        app.kubernetes.io/component: mariadb
    spec:
      securityContext:
        fsGroup: {{ .Values.mariadb.podSecurityContext.fsGroup | default 1001 }}
      containers:
        - name: mariadb
          image: "{{ .Values.mariadb.image.repository }}:{{ .Values.mariadb.image.tag }}"
          imagePullPolicy: {{ .Values.mariadb.image.pullPolicy }}
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.names.appName | default "smarter" }}-root-mariadb
                  key: mariadb-root-password
            - name: MYSQL_DATABASE
              value: {{ .Values.mariadb.auth.database }}
            - name: MYSQL_USER
              value: {{ .Values.mariadb.auth.username }}
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.names.appName | default "smarter" }}-user-mariadb
                  key: mariadb-password
          ports:
            - containerPort: 3306
              name: mariadb
          volumeMounts:
            - name: mariadb-data
              mountPath: /var/lib/mysql
          resources:
            {{- toYaml .Values.mariadb.resources | nindent 12 }}
          livenessProbe:
            {{- toYaml .Values.mariadb.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.mariadb.readinessProbe | nindent 12 }}
      volumes:
        - name: mariadb-data
          persistentVolumeClaim:
            claimName: {{ .Values.names.appName | default "smarter" }}-mariadb
