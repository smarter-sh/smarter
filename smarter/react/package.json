{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@smarter/common && npm run build --workspaces --exclude=@smarter/common"
  },
  "dependencies": {
    "recharts": "^3.9.1"
  }
}
