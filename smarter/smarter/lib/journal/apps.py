"""Django app configuration for the journal app."""

from django.apps import AppConfig


class JournalConfig(AppConfig):
    """Django app configuration for the journal app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "smarter.lib.journal"
