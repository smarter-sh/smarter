# -*- coding: utf-8 -*-
"""Django app configuration for the web platform app."""
from django.apps import AppConfig


class WebPlatformConfig(AppConfig):
    """Django app configuration for the web platform app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "smarter.apps.web_platform"
